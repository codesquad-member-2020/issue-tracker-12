/** @jsx jsx */
import { jsx, css } from '@emotion/core';
import Header from '@Components/Main/Header/Header';

const Main = () => {
  return <Header />;
};

const mainStyle = css`
  margin-top: 10vh;
`;
export default Main;
